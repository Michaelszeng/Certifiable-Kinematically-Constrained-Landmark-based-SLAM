pip installs:

- `pip install cvxpy`
- `pip install mosek`
- `pip install drake`
- `pip install pandas`
- `pip install seaborn`

Note: Mosek license needed. Also, drake mosek installation isn't trivial